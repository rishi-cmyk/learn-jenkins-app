pipeline{
    agent any
    stages{
        stage('Docker'){
            steps{
                sh 'docker build -f CI/Dockerfile-playwrite -t my-playwrite ./CI'
                sh 'docker build -f CI/Dockerfile-awsCLI -t my-awscli ./CI'

            }
            
        }
        
    }
}